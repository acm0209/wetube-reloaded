extends base

block content 
    if errorMessage 
        sapn = errormessage
    form(method="POST")
        input(placeholder="Name", name="username", type="text", required)
        input(placeholder="Email", name="email" , type="email", required)
        input(placeholder="Username", name="name", type="text", required)
        input(placeholder="Password", name="password", type="password", required)
        input(placeholder="Password", name="password2", type="password2", required)
        input(placeholder="Location", name="location", type="text", required)
        input(type="submit", value="Join")
    hr
    div 
        span Already have an acount? 
        a(href="/login") Login now &rarr;